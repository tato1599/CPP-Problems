#include <iostream>
#include <vector>

using namespace std;

int main(){
    vector<int> v ;
    vector<int> v2;
    int n,can,num,num2;
    cin>>n>>can;
    for (int i = 0; i < n; i++)
    {
        cin>>num;
        v.push_back(num);
        cin>>num2;
        v2.push_back(num2);
    }

  
    
    
    return 0;
}